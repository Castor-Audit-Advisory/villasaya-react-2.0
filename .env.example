# VillaSaya Environment Configuration
# Copy this file to .env and fill in your actual values

# ==============================================================================
# Supabase Configuration (REQUIRED)
# ==============================================================================
# Get these values from https://app.supabase.com/project/_/settings/api

# Your Supabase project ID (found in project settings or project URL)
VITE_SUPABASE_PROJECT_ID=your_project_id_here

# Your Supabase public anon key (safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# ==============================================================================
# Backend Environment Variables (for Edge Functions)
# ==============================================================================
# These must be configured as Supabase Edge Function secrets before deploying:
# - SUPABASE_URL: Your project's API URL
# - SUPABASE_SERVICE_ROLE_KEY: Service role key used by the Edge Function
# - SUPABASE_ANON_KEY: Public key for client-side operations and auth verification
#
# Set them with:
#   supabase secrets set SUPABASE_SERVICE_ROLE_KEY=your_service_role_key SUPABASE_ANON_KEY=your_anon_key

# ==============================================================================
# Optional Configuration
# ==============================================================================

# Node environment (development, production, test)
NODE_ENV=development

# Enable development-only endpoints (seed-test-users, etc.)
# Set to 'false' or remove entirely in production
ENABLE_DEV_ENDPOINTS=true
